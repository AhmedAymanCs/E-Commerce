class CartModel {
  double discount;
  double subtotal;
  double tax;
  double total;
  CartModel({
    required this.discount,
    required this.subtotal,
    required this.tax,
    required this.total,
  });
}
